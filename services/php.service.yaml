services:
  php:
    restart: unless-stopped
    build:
      context: ..
      target: production
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    environment:
      SSL_MODE: "off"
      AUTORUN_ENABLED: false
      PHP_DATE_TIMEZONE: "UTC"
      PHP_OPCACHE_ENABLE: true
